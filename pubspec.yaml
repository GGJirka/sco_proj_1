name: sco_proj_1
description: "Security research demo Flutter project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase – all versions aligned to firebase_core 4.x
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0

  # FIXED → updated to version compatible with firebase_core ^4.x
  cloud_functions: ^6.0.4

  firebase_app_check: ^0.4.1+2

  get_it: ^7.7.0
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  http: ^1.2.2
  crypto: ^3.0.5
  x509: ^0.2.4
  jailbreak_root_detection: ^1.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/config/mock_config.json
